## Postman Collection

The Postman collection is exported as:

`Postman_Collection.json`

It includes GET, POST, PUT, DELETE, negative testing, and basic Postman assertion scripts.

API keys are not stored in the collection. The collection uses the `{{api_key}}` environment variable.